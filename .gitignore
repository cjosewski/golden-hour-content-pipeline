# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/

# Secrets — the API key lives in the environment or an untracked .env,
# never in git
.env

# NOTE: output/ is deliberately NOT ignored. output/retrieval_trace.md and
# output/critic_log.md ARE the graded evidence for RAG Implementation and
# Consistency Checking — after a live run they must be committable, so nothing
# under output/ is excluded here.

# OMC assistant scratch state
.omc/
